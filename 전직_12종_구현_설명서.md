# 전직 12종 적용 설명서

## 게임에서 사용하는 방법

로비 → 캐릭터 → 기본 클래스 선택 → 전직 3종 살펴보기에서 전직합니다. 캐릭터 레벨 5부터 각 전직을 150골드와 8영혼으로 해금합니다. 해금한 전직으로 변경하는 비용은 없으며, 기본 클래스별 마지막 선택을 기억합니다. 전투 중에는 전직을 변경하지 않습니다.

검사: 용기사 / 광전사 / 기사단장. 마법사: 흑마법사 / 대마법사 / 마술사. 도적: 닌자 / 홍길동 / 해적. 성기사: 팔라딘 / 성전사 / 광마.

A는 기본 공격, Q는 전직별 짧은 스킬, W는 장비 스킬, E는 전직의 대표 스킬, R은 탐험에서 얻는 액티브입니다. 기본 공격은 A를 누르고 있을 때 반복됩니다.

## 흑마법사와 마술사의 룰렛

1. 준비 상태에서 E 또는 화면의 E 버튼을 누르면 1.2초 동안 룰렛이 돌아갑니다. 이때 재입력해도 즉시 발동하지 않습니다.
2. 추첨이 끝나면 결과 이름과 효과가 표시됩니다. 이 시점부터 10초 동안 보유합니다. 보유 중에는 E의 쿨타임이 흐르지 않습니다. 다른 스킬, 이동, 기본 공격은 가능합니다.
3. E를 다시 누르면 보유한 결과를 한 번 사용하고 쿨타임을 시작합니다.
4. 10초 안에 사용하지 않으면 결과를 버리고 쿨타임을 시작합니다. 시간 만료 시 자동으로 공격하지 않습니다.
5. 일시정지와 레벨업 선택 중에는 룰렛·보유 시간도 정지합니다. 사망 후에는 사용할 수 없으며 새 탐험을 시작하면 보유 결과를 초기화합니다.

흑마법사는 악마 소환, 저주의 씨앗, 되살린 대지를 같은 확률로 추첨합니다. 악마는 직선 범위를 공격하고, 씨앗은 지속 피해 장판을 만들며, 되살린 대지는 20초 동안 처치한 적의 위치에 회복 영역을 남깁니다. 겹친 회복 영역의 회복량은 중복되지 않습니다.

마술사는 에이스 또는 조커를 같은 확률로 추첨합니다. 에이스는 카드 폭풍으로 피해·밀치기·감속을 주고 E 쿨타임을 2~5% 반환합니다. 조커의 복제 대상은 추첨이 끝날 때 확정되며 보유 화면에서 확인할 수 있습니다. 복제한 스킬은 마술사의 공격력으로 계산하고 E 쿨타임 30초를 사용합니다.

## 외형과 동작

`image/advancements_20260912`에 24개 PNG를 저장했습니다. 전직별 `_motion.png`는 대기·준비·발동·회복의 4포즈 시트이며 `_skill.png`는 스킬 아이콘입니다. 투명 배경의 2D 캐릭터이고 3D 메시나 리깅 파일은 아닙니다. 이동은 대기 포즈의 보행 흔들림, 좌우 방향은 반전, 피격·사망은 코드 변형으로 처리합니다.

시트마다 생성된 포즈 폭이 조금 달라 `ADV_FRAME_EDGES`로 프레임 경계를 지정했습니다. 시트를 교체한다면 경계값과 렌더링 크기를 함께 확인해야 합니다. 스킬 범위, 번개, 해일, 분신, 방벽, 카드 궤적은 Canvas에서 게임 시간에 맞춰 그립니다.

모든 이미지는 내장 이미지 생성 도구로 제작했습니다. 각 PNG 옆 `.png.json`에 사용한 프롬프트와 원본 저장 위치를 기록했습니다. 원본은 별도로 보관하고 게임은 프로젝트 내 파일을 읽습니다.

## 코드 수정 위치

- `advancement-data.js`: 전직 이름, 계열, 해금 비용, 능력치, 기본 공격 설명, Q/E 이름과 쿨타임, 아이콘·동작 경로.
- `advancements.js`: 전직 초기화, 기본 공격, Q/E 실행, 룰렛 상태, 소환수·장판·버프 갱신, 이미지 프레임과 스킬 연출, 전직 선택 화면.
- `meta.js`: 전직 해금·선택 저장과 데이터 검사. 저장 실패 시 해금 비용과 선택 상태를 되돌립니다.
- `game.js`: 전직 기능을 기존 입력·피해·처치·업데이트·그리기·HUD에 연결합니다.
- `advancement.css`: 전직 카드, 모바일 배치, 룰렛 보유 표시.
- `index.html`: 새 파일 로딩 순서. 전직 기능을 로비보다 먼저 불러와야 합니다.
- `sw.js`: 오프라인 캐시 목록과 버전. 배포 시 변경한 파일이 오래된 캐시에 남지 않도록 버전을 갱신합니다.

피해량은 대부분 현재 공격력의 배수입니다. `executeAdvUltimate`와 `executeAdvQ`에서 범위·지연·지속 시간·배수를 조정할 수 있습니다. `updateAdvancements(dt)`는 게임이 진행 중일 때만 실행됩니다. 전투 중 예약 공격에 실제 시간의 `setTimeout`을 사용하지 않습니다.

## 밸런스와 저장 호환

광전사는 체력 30% 이하에서만 E를 직접 발동하며 보호막으로 위험을 완화합니다. 기사단장은 독립적으로 추격하는 기사 최대 3명, 홍길동은 기본 공격에 반응하는 분신 최대 2명으로 구분합니다. 광마는 보스 적중으로도 중첩을 쌓을 수 있고 광기는 최대 20중첩·6초, 처치 기반 영구 성장은 해당 탐험에서 최대 5회로 제한합니다.

기존 저장의 레벨·재화·장비·펫·이전 전직 기록은 유지합니다. 새 전직을 선택하면 새 전직의 능력치와 스킬을 적용합니다. 이전 전직 보너스를 이중으로 더하지 않습니다. 새 전직 선택이 없는 경우 기존 전직을 계속 사용합니다.

## 사운드

기존 프로젝트의 타격, 마법탄, 화염, 번개 사운드를 연결하고 룰렛 시작·확정에는 짧은 합성 알림음을 사용합니다. 전용 녹음 파일은 새로 생성하지 않았습니다. 향후 용의 포효, 악마 등장, 룰렛 감속·확정, 카드 넘김, 해일·선박, 기사 소환, 혈마권의 전용 효과음을 추가하면 전직 간 구분이 더 선명해집니다.

## 검증

`tests/advancements.cjs`는 독립된 브라우저 저장소에서 전직 12종의 아트 로딩, 기본 공격, Q/E, 룰렛 보유·만료·재입력, 소환수, 버프, 저장 호환을 확인합니다. 사용자의 실제 브라우저 진행도를 변경하지 않습니다. `test-results/advancement-checks.json`에 실행 결과, 같은 폴더의 `advancements-desktop.png`, `advancements-mobile.png`, `roulette-mobile.png`에 화면을 기록합니다.

피해 배수는 실제 난이도에 맞추기 위한 초기값입니다. 장시간 플레이를 통한 전직별 클리어율·보스 처치 시간 조정은 별도의 밸런스 작업입니다.
